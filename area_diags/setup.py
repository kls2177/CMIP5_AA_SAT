from setuptools import setup, find_packages
setup(
    name = "area_diags", #proper style -> no uppercase letters
    version = "0.1",
    #packages = find_packages(),
    packages = ['area_diags'] #equivalent to above line
)
